#include <stdio.h>

int main(){
    int a,b,s;

    printf("Escrava dois valores divididos por 1 espaco");
    scanf("%d%d",&a,&b);
    s=a+b;
    printf ("A soma do valor %d + %d é igual %d",a,b,s);
    return 0;
}